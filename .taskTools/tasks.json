[
  {
    "taskNumber": 35,
    "title": "Activate the new manifest and workflow version after the cutover suite is green",
    "description": "Final step of the recursive repository-discovery cutover, after the split into tasks 45 to 51.\n\nEvery call-site migration now lives in its own task: 45 manifest bootstrap, 46 repositoryBranches, 47 taskGroups, 48 prepareTasks, 49 mergeTaskWorktrees finalization, 50 workflow and SKILL approval split, 51 legacy manifest routing. This task is only the version flip that activates the new manifest and workflow version once all of them have landed.\n\nFlip the version only after the complete end-to-end suite passes: a full node --test tests/ run plus npx tsc --noEmit, both green, in the same commit. Isolate the flip as its own commit so it can be reverted without unwinding the migration.\n\nThis task was attempted three times as a single whole-cutover unit and refused each time for a different verified reason: detached-HEAD resolution semantics, discoverRepositoryTree checking out working directories (fixed by task 44), and the absence of any manifest bootstrap (now task 45). Do not re-merge the split steps back into this task.\n\nTests: the version constant advances only with the full suite green, and a guard refuses the flip on any nonzero exit.",
    "files": [
      "scripts/repositoryManifest.ts",
      "tests/repositoryManifest.test.ts"
    ],
    "handoffFilePaths": [
      "plans/codex-tackle-tasks-repo-discovery-phase-4.md"
    ],
    "difficulty": 3
  },
  {
    "taskNumber": 36,
    "title": "RevEng-shaped end-to-end fixture and Phase 4 acceptance run",
    "description": "Phase 4 of the recursive repository-discovery redesign — the final acceptance gate.\n\nBuild a RevEng-shaped end-to-end fixture in tests/fixtures/revengGraph.ts containing tmux_lib at tmux_lib, jfred/external/tmux_lib, and jfred/jfredToolsPlugin/external/tmux_lib; two occurrences each of claude_plugin_lib and scenarios; nested submodules beneath ordinary directories; and distinct parent chains. Drive a full run against it in tests/revengEndToEnd.test.ts.\n\nVerify in that fixture that a change originating from any repeated occurrence converges everywhere; all child and ancestor suites pass; approval precedes every commit, merge, and push; all occurrence histories consolidate into one operation per logical repository; gitlinks propagate through the correct explicit parents to the root; and every intended local base ends at the expected merge commit. Also verify no semantic commit, push, integration merge, base mutation, or archival occurs before approval, and that only successfully published tasks are archived.\n\nPhase 4 acceptance: the recursive workflow is the production path, completes the RevEng-shaped run without data loss, and archives only tasks whose integration was successfully published.",
    "files": [
      "tests/revengEndToEnd.test.ts",
      "tests/fixtures/revengGraph.ts"
    ],
    "handoffFilePaths": [
      "plans/codex-tackle-tasks-repo-discovery-phase-4.md"
    ],
    "blockedBy": [
      35
    ],
    "difficulty": 4
  },
  {
    "taskNumber": 58,
    "title": "Split task file ownership into modifiableFiles and readOnlyFiles, and make briefs point at files instead of pasting them",
    "description": "When preparing the brief for a task, use file pointers instead of copying file contents into the brief. Add 'readOnlyFiles' to the keys of each task to provide the list of files to be read by the agent, with 'readOnlyFiles: [*]' meaning every file in the project is readable by the subagents. Change 'files' to 'modifiableFiles', meaning 'only these files are editable by the subagents'. When a task only has 'files' but not 'modifiableFiles', accept 'files'. When 'readOnlyFiles' is missing, default to '[*]' meaning all project files are readable. When close-tasks is invoked, replace 'files' with 'modifiableFiles' in the project's tasks.json.\n\nConcretely:\n\n1. scripts/prepareTasks.ts writeTaskBriefFile (around lines 103-120) currently inlines every declared file's full contents into plans/brief-N.md inside a fenced block. Replace those sections with a pointer list: the modifiable paths under one heading and the readable paths under another, no file bodies. Note '(missing: file not found on disk)' still applies to a declared path that does not exist.\n\n2. Add one shared accessor pair used everywhere a task's file fence is read: modifiableFiles falls back to files when absent, and readOnlyFiles defaults to ['*']. The existing readers are scripts/prepareTasks.ts declaredFiles (line 100) and scripts/taskGroups.ts declaredFiles (line 16); both must route through it so grouping and preparation cannot diverge. Grouping and worktree-scope decisions key off modifiableFiles only — readOnlyFiles must never widen a group or make two tasks collide.\n\n3. Thread readOnlyFiles through the prepared-task shape so the workflows can see it: the PreparedTask interface in scripts/prepareTasks.ts (line 17 files: string[]) and its construction site (line 170 files: group.filePaths), the ApprovalGate task shape in scripts/approvalGate.ts (line 12), and the flatten in scripts/mergePipeline.ts (line 141, task.files).\n\n4. Update the four workflow prompts so the edit fence and the read fence are stated separately. Today they conflate the two: skills/tackle-tasks/plan.workflow.js line 28 says 'You may also READ these owned files, and nothing else'; implement.workflow.js lines 38 and 73; test.workflow.js lines 49, 86 and 104; verify.workflow.js line 25. Only modifiableFiles may be edited or git-added; readOnlyFiles (or the whole project when it is ['*']) may be read.\n\n5. Update the task-authoring surfaces to emit and maintain the new keys: skills/create-task/template/taskTemplate.json, skills/create-task/SKILL.md, and skills/update-task-files/SKILL.md.\n\n6. Add a migration step to skills/close-tasks/SKILL.md: on each invocation, rename any remaining 'files' key to 'modifiableFiles' across the project's tasks.json, so the legacy key drains out over time even though readers still accept it.\n\nThe motivation for the read fence is real: task 50's planner produced a plan against a file that no longer existed because its read fence was scoped to its own owned files. Widening reads while keeping writes narrow fixes that without loosening ownership.",
    "files": [
      "scripts/prepareTasks.ts",
      "scripts/taskGroups.ts",
      "scripts/approvalGate.ts",
      "scripts/mergePipeline.ts",
      "skills/tackle-tasks/plan.workflow.js",
      "skills/tackle-tasks/implement.workflow.js",
      "skills/tackle-tasks/test.workflow.js",
      "skills/tackle-tasks/verify.workflow.js",
      "skills/create-task/SKILL.md",
      "skills/create-task/template/taskTemplate.json",
      "skills/update-task-files/SKILL.md",
      "skills/close-tasks/SKILL.md",
      "tests/prepareTasks.test.ts",
      "tests/prepareTasksIntegration.test.ts",
      "tests/taskGroups.test.ts"
    ],
    "tests": "Prepare a task declaring modifiableFiles plus readOnlyFiles, and a second legacy task declaring only files. Assert both group and prepare identically, that each brief lists its file paths and contains no file contents, and that the task omitting readOnlyFiles is treated as [*].",
    "difficulty": 4
  },
  {
    "taskNumber": 59,
    "title": "Refuse tackle-tasks with a clear message in a repository that has no origin remote",
    "description": "Repos without an origin crash the tackle-tasks skill. When tackle-tasks is invoked in a repo without an origin, report an error to the user \"this repository does not have an origin remote. set one to continue to use 'tackle-tasks'\".\n\nObserved failure, running /taskTools:tackle-tasks [3] valid in a repository with no origin remote:\n\n  error: No such remote 'origin'\n  Error: occurrence \"\" has an unparseable origin URL: \"rootcommit:86b6e87d544536cf1d9009a3a11dd7268e4ad38e\"\n      at buildLogicalRepositories (scripts/logicalRepository.ts:54:19)\n      at findLogicalRepository (scripts/ownershipKeys.ts:19:33)\n      at computeCanonicalOwnershipKey (scripts/ownershipKeys.ts:29:31)\n      at expandTaskPathEffects (scripts/ownershipKeys.ts:54:35)\n      at canonicalKeysForTask (scripts/canonicalTaskGroups.ts:15:25)\n      at buildCanonicalTaskGroups (scripts/canonicalTaskGroups.ts:46:61)\n      at groupTasksByFileOverlap (scripts/taskGroups.ts:61:12)\n      at runAsCli (scripts/prepareTasks.ts:198:20)\n\nRoot cause, verified: scripts/repositoryDiscovery.ts readOriginUrl (lines 26-37) catches the failure of `git remote get-url origin` and substitutes a synthetic identity, `rootcommit:<root commit oid>`, as the occurrence's originUrl. Its only caller is line 116. scripts/submoduleUrlIdentity.ts normalizeRepositoryIdentity (line 62) has no case for that scheme: the scp-style branch matches on the colon, leaves a single path segment, hits the `segments.length < 2` guard on line 88 and returns null. scripts/logicalRepository.ts buildLogicalRepositories (line 54) then throws on the null. Confirmed directly: normalizeRepositoryIdentity(\"rootcommit:86b6e87d544536cf1d9009a3a11dd7268e4ad38e\") returns null while a real origin URL resolves normally.\n\nThe decision is to refuse rather than to support origin-less repositories, so do NOT teach normalizeRepositoryIdentity the rootcommit scheme. Detect the missing origin early and refuse with the exact sentence above.\n\nThe message must reach the user as a plain one-line error, not a stack trace. scripts/prepareTasks.ts runAsCli already has the right pattern at lines 190-195: it wraps selectRequestedTasks in a try/catch that writes `prepareTasks: <message>` to stderr and exits 1. The crashing calls, loadRepositoryManifest and groupTasksByFileOverlap on lines 197-198, sit outside that guard, which is why the raw trace escaped. Put the origin check where it fires before any brief or worktree is written, and route it through the same clean stderr-and-exit-1 path.\n\nConsider whether readOriginUrl's rootcommit fallback should remain at all once the entry point refuses these repositories; if it stays, it must not be able to reach buildLogicalRepositories.",
    "files": [
      "scripts/prepareTasks.ts",
      "scripts/repositoryDiscovery.ts",
      "tests/prepareTasks.test.ts",
      "tests/repositoryDiscovery.test.ts"
    ],
    "tests": "Init a throwaway git repo with a commit and no origin remote, run prepareTasks.ts against it, and assert exit code 1, stderr containing the exact sentence, and no stack trace or the word rootcommit anywhere in the output.",
    "difficulty": 2
  },
  {
    "taskNumber": 60,
    "title": "Split a task's raw user prompt into userDescription, separate from the agent-derived description",
    "userDescription": "when using 'create-task', take the user's description and add it to the task as key: 'userDescription', separate from the 'description' key.  the description key should be the derived/flushed out description generated by the agent.  this separates the user's prompt from the agent's understanding. the current behavior is to put the user's description at the beginning of the 'description' field and then put two new lines and then put the agent's understanding of the task.",
    "description": "Give a task record two distinct fields instead of one overloaded one. 'userDescription' holds the user's raw prompt verbatim, exactly as typed. 'description' holds only the agent's derived, fleshed-out understanding: file paths, line numbers, root-cause findings, constraints and decisions gathered while writing the task.\n\nToday the two are concatenated into 'description' — the user's wording first, then a blank line, then the agent's expansion — so there is no way to tell afterwards which sentences the user actually wrote and which the agent inferred. That matters when a task is re-read months later and the agent's inference turns out to have been wrong: the user's original intent is the thing that must survive intact.\n\nChanges:\n\n1. skills/create-task/template/taskTemplate.json — add the 'userDescription' key alongside 'description'.\n\n2. skills/create-task/SKILL.md — instruct that 'userDescription' is populated with $ARGUMENTS verbatim and never edited, summarized or reworded, and that 'description' carries only the derived understanding and must not restate the raw prompt.\n\n3. Both existing readers of 'description' must surface the new field under its own heading rather than silently dropping it: scripts/prepareTasks.ts writeTaskBriefFile (line 114, task.description ?? \"\") and scripts/viewTaskHook.ts (line 11). Treat 'userDescription' as optional, since every existing task in tasks.json and completedTasks.json predates it and has only 'description'.\n\nNo migration of existing task records is required or wanted — old tasks keep their combined 'description' and simply have no 'userDescription'.\n\nNote for scheduling: this task edits skills/create-task/SKILL.md and skills/create-task/template/taskTemplate.json, which task 58 also edits, so the two will be grouped into the same worktree rather than run in parallel.",
    "files": [
      "skills/create-task/SKILL.md",
      "skills/create-task/template/taskTemplate.json",
      "scripts/prepareTasks.ts",
      "scripts/viewTaskHook.ts",
      "tests/prepareTasks.test.ts"
    ],
    "tests": "Create a task from a short raw prompt, then assert userDescription is that prompt verbatim, description is the agent's expanded version and does not merely repeat the prompt, and that both the generated brief and the viewTaskHook output show each field under its own heading.",
    "difficulty": 2
  },
  {
    "taskNumber": 61,
    "title": "Have the commit-message subagent collect the staged diff itself instead of receiving it from the main agent",
    "userDescription": "make the commit-message subagent always get the latest git changes programmatically, instead of from the main agent, because the main agent's repo view goes stale after 1 conversation turn.",
    "description": "The commit-message subagent must run the git commands itself, at the moment it starts, rather than being handed a diff the main agent captured earlier. The main agent's view of the repository goes stale as soon as anything else is staged, so a diff passed in as prompt text describes a snapshot that may no longer match HEAD's index.\n\nObserved in a real run of this pipeline: the commit-message subagent was launched, and tasks 58, 59 and 60 were staged afterwards. The returned message described neither of them and mischaracterised the code change it did see. Nothing errored — the message was simply wrong, which is the dangerous failure mode, because a wrong-but-plausible commit message gets used.\n\nThe wording that causes this is in skills/tackle-tasks/COMMIT_MESSAGES.md: \"give it the staged diff of every affected repo — collected after staging with git -C <repo> diff --staged ...\". \"Give it\" puts the collection in the caller's hands and makes the diff prompt text. Rewrite so the subagent is instructed to run `git -C <repo> diff --staged -- ':(exclude)*tasks.json' ':(exclude)*completedTasks.json' ':(exclude)plans/archived'` itself, along with whatever it needs to enumerate the affected repos and detect a moved submodule pointer. The caller should pass repository paths, not diff contents.\n\nskills/update-tasks/SKILL.md line 27 has the same defect and is vaguer still — it says to use a Sonnet 5 subagent to summarise \"the work done\" without saying where the diff comes from at all. Point it at COMMIT_MESSAGES.md rather than restating the procedure, so there is one source of truth.\n\nNote that scripts/stage-and-summarize-stop.ts (line 36) inlines COMMIT_MESSAGES.md into a Stop hook, and skills/tackle-tasks/SKILL.md (line 141) does the same, so both pick up the corrected wording automatically with no edit of their own. skills/close-tasks/SKILL.md line 22 is unaffected: it uses a fixed \"Closed tasks [...]\" message and no subagent.",
    "files": [
      "skills/tackle-tasks/COMMIT_MESSAGES.md",
      "skills/update-tasks/SKILL.md",
      "tests/commitMessageSubagent.test.ts"
    ],
    "tests": "in a throwaway repo, stage a change, spawn commit-message subagent, then make the commit with the subagent's message, then make another change, stage that 2nd change, spawn commit-message subagent again, and make sure the subagent's message is different (because the diff contents are different)",
    "difficulty": 2
  }
]
