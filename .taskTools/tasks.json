[
  {
    "taskNumber": 35,
    "title": "Activate the new manifest and workflow version after the cutover suite is green",
    "description": "Final step of the recursive repository-discovery cutover, after the split into tasks 45 to 51.\n\nEvery call-site migration now lives in its own task: 45 manifest bootstrap, 46 repositoryBranches, 47 taskGroups, 48 prepareTasks, 49 mergeTaskWorktrees finalization, 50 workflow and SKILL approval split, 51 legacy manifest routing. This task is only the version flip that activates the new manifest and workflow version once all of them have landed.\n\nFlip the version only after the complete end-to-end suite passes: a full node --test tests/ run plus npx tsc --noEmit, both green, in the same commit. Isolate the flip as its own commit so it can be reverted without unwinding the migration.\n\nThis task was attempted three times as a single whole-cutover unit and refused each time for a different verified reason: detached-HEAD resolution semantics, discoverRepositoryTree checking out working directories (fixed by task 44), and the absence of any manifest bootstrap (now task 45). Do not re-merge the split steps back into this task.\n\nTests: the version constant advances only with the full suite green, and a guard refuses the flip on any nonzero exit.",
    "files": [
      "scripts/repositoryManifest.ts",
      "tests/repositoryManifest.test.ts"
    ],
    "handoffFilePaths": [
      "plans/codex-tackle-tasks-repo-discovery-phase-4.md"
    ],
    "blockedBy": [
      48,
      49,
      50,
      51
    ]
  },
  {
    "taskNumber": 36,
    "title": "RevEng-shaped end-to-end fixture and Phase 4 acceptance run",
    "description": "Phase 4 of the recursive repository-discovery redesign — the final acceptance gate.\n\nBuild a RevEng-shaped end-to-end fixture in tests/fixtures/revengGraph.ts containing tmux_lib at tmux_lib, jfred/external/tmux_lib, and jfred/jfredToolsPlugin/external/tmux_lib; two occurrences each of claude_plugin_lib and scenarios; nested submodules beneath ordinary directories; and distinct parent chains. Drive a full run against it in tests/revengEndToEnd.test.ts.\n\nVerify in that fixture that a change originating from any repeated occurrence converges everywhere; all child and ancestor suites pass; approval precedes every commit, merge, and push; all occurrence histories consolidate into one operation per logical repository; gitlinks propagate through the correct explicit parents to the root; and every intended local base ends at the expected merge commit. Also verify no semantic commit, push, integration merge, base mutation, or archival occurs before approval, and that only successfully published tasks are archived.\n\nPhase 4 acceptance: the recursive workflow is the production path, completes the RevEng-shaped run without data loss, and archives only tasks whose integration was successfully published.",
    "files": [
      "tests/revengEndToEnd.test.ts",
      "tests/fixtures/revengGraph.ts"
    ],
    "handoffFilePaths": [
      "plans/codex-tackle-tasks-repo-discovery-phase-4.md"
    ],
    "blockedBy": [
      35
    ]
  },
  {
    "taskNumber": 39,
    "title": "merge-worktree-tasks skill",
    "description": "a way to check if worktrees weren't merged and if there are open tasks that haven't been completed and are assigned to those worktrees, and then to run the merge-worktrees script to merge them *if the task is approved*.  Cause: sometimes I forget to commit when tasks are completed, which blocks the worktrees from being commited. \n\nWorktrees live at `tmpdir()/taskTools-wt/<basename(repoRoot)>/group-<groupId>` on branch `task-group-<groupId>`, but groupId is recomputed every prepareTasks run and never persisted, so a leftover worktree cannot be matched to task numbers by groupId. Match by file overlap instead: read the worktree branch's changed files plus its uncommitted status and compare them against `declaredFiles(task)` from scripts/taskGroups.ts for open tasks.\n\nExtend scripts/mergeTaskWorktrees.ts rather than adding a new script: add `listTaskWorktrees(repoRoot)` (parse `git worktree list --porcelain`, keep only paths matching the taskTools-wt convention), `findUnmergedTaskWorktrees(repoRoot, sourceBranch, openTasks)` (unmerged-commit count plus uncommitted status, changed files matched to declared files), and CLI modes `--discover` and `--merge <worktree>` reusing the existing collectRepositorySources, currentBranchName, mergeGroupBranchIntoRepo and removeWorktreeAndBranch. Create skills/merge-worktree-tasks/SKILL.md: run --discover, report each unmerged worktree with its matched open task numbers, gate the merge behind explicit user approval via AskUserQuestion, then run --merge only for approved worktrees.",
    "files": [
      "scripts/mergeTaskWorktrees.ts",
      "skills/merge-worktree-tasks/SKILL.md"
    ],
    "handoffFilePaths": [],
    "blockedBy": []
  },
  {
    "taskNumber": 41,
    "title": "add 'verify plan' and 'test' to tackle-tasks workflow",
    "description": "the workflow does plan -> implement -> typecheck -> merge. it needs to add 'verify' after plan, and then 'test' after 'implement'.  'verify' is a separate agent that checks if the plan is good or not.  'test' is running the tests for the files touched by the implementation, and then reporting the test results to the implementing agent to fix or not.\n\nIn skills/tackle-tasks/tackle-tasks.workflow.js add a Verify phase (one agent per task after Plan, gating which planned tasks reach Implement) and a Test phase (one agent per group after Implement, feeding failures back into runWorker the way the existing partial-requeue loop does). Both need meta.phases entries, result arrays threaded into the final return, and an extra pipeline() stage.\n\nThe file is 194 of the 250-line cap and the two phases add roughly 60-100 lines, so split first: move VERIFY_SCHEMA, TEST_SCHEMA and the verifierBrief/testerBrief prompt builders (plus the existing plannerBrief/workerBrief for consistency) into a new skills/tackle-tasks/tackle-tasks.briefs.js imported by the workflow, leaving the workflow as thin orchestration wiring. The Test agent brief should point at the existing scripts/relatedTests.ts discovery pattern rather than duplicating test discovery; relatedTests.ts and testPolicy.ts themselves need no changes.",
    "files": [
      "skills/tackle-tasks/tackle-tasks.workflow.js",
      "skills/tackle-tasks/tackle-tasks.briefs.js"
    ],
    "handoffFilePaths": [],
    "blockedBy": []
  },
  {
    "taskNumber": 43,
    "title": "Rewrite pick-a-task's selection criteria to use the difficulty field",
    "description": "Revisit how pick-a-task chooses tasks once the `difficulty` field is supported on tasks.json entries.\n\nToday skills/pick-a-task/SKILL.md picks the N easiest open tasks by having the agent read every task and reason in prose about which are simplest, comparing each against the current state of the project. Once each task carries a difficulty rating (1-5), most of that reasoning becomes a sort: filter to unblocked, order by difficulty ascending, take N.\n\nRewrite the picking criteria to use the field, and decide what judgment is still worth spending prose on. The remaining candidate is the relevance check — is this task still needed given the current codebase? — which is separate from difficulty and cannot be sorted.\n\nBlocked by the task that adds the difficulty field to tasks.json entries and to skills/create-task/template/taskTemplate.json; that task does not exist yet, so add its number to blockedBy once it is created.",
    "files": [
      "skills/pick-a-task/SKILL.md"
    ],
    "handoffFilePaths": [],
    "blockedBy": []
  },
  {
    "taskNumber": 48,
    "title": "Rewire prepareTasks call sites onto graph discovery and canonical grouping",
    "description": "Step 3 of the task 35 cutover split.\n\nPoint scripts/prepareTasks.ts at the rewired helpers: buildWorkflowArguments and createWorktreeForGroup take their discovery and branch creation from task 46, and runAsCli takes its grouping from task 47. Thread through whatever new WorkflowArguments and PreparedGroup fields the approval split needs, so the workflow can carry graph metadata, recovery refs and receipts later.\n\nbuildWorkflowArguments is called against the production repo root before any worktree exists, so it must stay read-only with respect to branches — no checkout as a side effect of preparing.\n\nTests: tests/prepareTasks.test.ts test_buildWorkflowArgumentsRefusesADetachedSubmoduleWithoutCreatingAWorktreeDirectory must keep passing unedited, and the whole existing suite stays green.\n\nRequired: replace the synthesized manifest stub left behind by task 47. scripts/taskGroups.ts currently calls buildCanonicalTaskGroups with a locally fabricated buildFlatSingleRepositoryManifest — one occurrence with occurrenceId flat, originUrl https://local/flat/flat.git, a baseOid of forty zeros and baseBranch main — because the task 47 worker owned only taskGroups.ts and could not reach a repoRoot. That stub keeps grouping effectively flat, so submodules are still not grouped by real logical repository. This task owns prepareTasks.ts, has the repoRoot, and must thread the real manifest from bootstrapRepositoryManifest through to grouping and delete buildFlatSingleRepositoryManifest along with the stale comment claiming no disk-free manifest constructor exists. The cutover is not complete while a fabricated manifest sits in the hot path.\n\nREOPENED. The first attempt (commit 1039a4e) merged with a failing typecheck and broke tackle-tasks entirely — prepareTasks could not group any task — and was reverted in commit fbe32a7, along with the computeTaskStats manifest parameter it forced. Do not re-attempt until task 52 lands an end-to-end integration test against a real repository and fixes the discovery defects it documents: absolute checkoutPath versus root-relative task paths, and an originUrl that is never read from the git remote.",
    "files": [
      "scripts/prepareTasks.ts",
      "tests/prepareTasks.test.ts",
      "scripts/taskGroups.ts",
      "tests/taskGroups.test.ts"
    ],
    "blockedBy": [
      52
    ]
  },
  {
    "taskNumber": 49,
    "title": "Drive mergeTaskWorktrees through the finalization and publication pipeline",
    "description": "Step 4 of the task 35 cutover split, and its largest behavior change.\n\nRewire scripts/mergeTaskWorktrees.ts runAsCli to drive scripts/runFinalizer.ts, scripts/runConsolidation.ts, scripts/operationPush.ts, scripts/basePublication.ts and scripts/taskArchival.ts. Read all five in full before editing.\n\nCLI contract decision already made by the user — implement it, do not re-litigate: the script keeps its current front door. It accepts the flat WorkflowArguments JSON as argv[2] and writes the same stdout shape it writes today. Translate the flat arguments into the graph and occurrence shapes INSIDE the script. Every existing test in tests/mergeTaskWorktrees.test.ts must keep passing unchanged, with no rewritten CLI assertions. A graph-shaped front door is a later follow-up.\n\nKeep mergeGroupBranchIntoRepo, mergeSubmoduleBranchIntoRepo, resolveGitlinkConflicts and removeWorktreeAndBranch exported and behaviorally unchanged.\n\nTests: the existing suite passes unedited; the finalization path is exercised end to end against a temporary repository.",
    "files": [
      "scripts/mergeTaskWorktrees.ts",
      "tests/mergeTaskWorktrees.test.ts"
    ],
    "blockedBy": [
      48
    ]
  },
  {
    "taskNumber": 50,
    "title": "Split the tackle-tasks workflow into prepare-for-approval and finalize stages",
    "description": "Step 5 of the task 35 cutover split.\n\nRewire skills/tackle-tasks/tackle-tasks.workflow.js and skills/tackle-tasks/SKILL.md so preparation returns recursive graph metadata, hook mode, recovery refs, test receipts, review handoffs and readyForApproval instead of immediate merge results, and so the whole-run approval gate runs before finalization.\n\nUse the task 31 modules: scripts/approvalGate.ts, scripts/approvalReadiness.ts and scripts/runAuthorization.ts. There is exactly one approval in a run — do not introduce a second post-merge gate. Replace MERGE_SCHEMA with a shape carrying readyForApproval, and update the SKILL.md flow so the orchestrator presents the gate before anything is finalized or archived.\n\nTests: the workflow returns readyForApproval rather than merge results before approval; finalization runs only with a valid authorization.",
    "files": [
      "skills/tackle-tasks/tackle-tasks.workflow.js",
      "skills/tackle-tasks/SKILL.md"
    ],
    "blockedBy": [
      49
    ]
  },
  {
    "taskNumber": 51,
    "title": "Route legacy manifests to a non-destructive refusal with recovery instructions",
    "description": "Step 6 of the task 35 cutover split.\n\nDispatch legacy versus new manifests through scripts/legacyManifest.ts at the mergeTaskWorktrees.ts runAsCli entry point, routing a legacy manifest to its existing compatible tooling or to a non-destructive refusal that reports exact recovery instructions.\n\nLegacy worktrees must survive the refusal untouched — nothing is removed, reset or force-checked-out on this path.\n\nTests: a legacy manifest is refused non-destructively, its recovery instructions name the affected worktree, and the worktree still exists on disk afterwards.",
    "files": [
      "scripts/legacyManifest.ts",
      "scripts/mergeTaskWorktrees.ts",
      "tests/legacyManifest.test.ts"
    ],
    "blockedBy": [
      49
    ]
  },
  {
    "taskNumber": 52,
    "title": "End-to-end integration test: prepareTasks against a real repository",
    "description": "Phase 1 to 3 was unit-tested entirely against hand-written manifest fixtures and never executed against a real repository, so wiring it into production (task 48) broke tackle-tasks completely and was reverted in commit fbe32a7. Three integration defects were found and none were caught by the 1027-test suite.\n\n1. scripts/repositoryDiscovery.ts line 72 records checkoutPath as an absolute path via join(rootPath, relativePath), while scripts/repositoryGraph.ts isWithinCheckout matches it against root-relative task paths like scripts/foo.ts — so nothing owns any path and prepareTasks throws: no occurrence owns path scripts/mergeTaskWorktrees.ts. Decision already made by the user: absolute wins, and the graph matchers relativize against the root occurrence checkoutPath. Do not re-litigate that.\n\n2. scripts/repositoryDiscovery.ts line 103 sets originUrl to existing?.originUrl ?? empty string and never reads the git remote, but logical repository identity is keyed on originUrl, so ownership fails with: occurrence has an unparseable origin URL.\n\n3. The root occurrence is given an empty occurrenceId.\n\nAlso reconcile the coordinate systems: tests/repositoryGraph.test.ts uses a fixture whose task paths are relative to the root occurrence PARENT directory (root checkoutPath jfred, paths like jfred/external/tmux_lib/src/foo.ts), while production task paths are relative to the repo root itself. Those two conventions conflict and one of them has to give.\n\nAdd tests/prepareTasksIntegration.test.ts that builds a real temporary git repository with an origin remote and at least one submodule, then runs bootstrapRepositoryManifest, groupTasksByFileOverlap and buildWorkflowArguments against it and asserts: ownership resolves for a root file path and for a submodule file path; every occurrence carries a non-empty originUrl and occurrenceId; grouping returns real groups instead of throwing.\n\nFix scripts/repositoryDiscovery.ts and scripts/repositoryGraph.ts as needed to make it pass. This must land before task 48 is re-attempted.",
    "files": [
      "tests/prepareTasksIntegration.test.ts",
      "scripts/repositoryDiscovery.ts",
      "scripts/repositoryGraph.ts",
      "tests/repositoryGraph.test.ts"
    ]
  }
]
