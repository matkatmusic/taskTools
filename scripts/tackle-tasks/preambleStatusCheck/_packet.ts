// One shape for every green block between PREAMBLE_STATUS_CHECK and CODEX_REVIEWS_PLAN, so no edge has a dialect.
export type EntryPacket = {
    box: string;
    scriptSignal: string;
    taskNumber: number;
    runId: string;
    projectRoot: string;
    worktree: string;
    branch: string;
    docsMode: string;
    planFile: string;
    exitType: string;
    exitNote: string;
};
