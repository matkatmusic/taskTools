# tackle-tasks blocker audit (HEAD of `new-run-step-tool-integration`, taskTools-86)

| # | blocker | fixed in HEAD? (evidence) | regression test? | breaks in another repo? (evidence) |
|---|---|---|---|---|
| 1 | Duplicate hook registration inflating clarify counters | **Fixed.** Commit `72deb79` removed the duplicate hook block from `.claude/settings.json` (diff: `-"hooks": {...31 lines...}` → `+"hooks": {}`). On disk, `.claude/settings.json` now has `"hooks": {}`; `hooks/hooks.json` is the sole registrar. | **NONE.** `tests/hookInstalled.test.ts:11-19` only asserts the hook fires in *at least one* file (`.some(...)`) — it would still pass if both files registered it again. | Low. The old duplicate hardcoded an absolute path unique to this checkout (`/Users/.../taskTools-86/scripts/...`), so a fresh target repo wouldn't inherit it verbatim — but since (b) is NONE, nothing stops re-introduction in any repo. |
| 2 | clarifyRequest never reaching the planner | **NOT fixed.** `scripts/tackle-tasks/planTheTask/PLAN_THE_TASK.ts:28` and `:62` call `planPrompt(t)` / `planPrompt(prepared)` with no `extra` argument, so `PlanPromptExtra.clarifyRequest` (`scripts/tackle-tasks/shared/planPrompt.ts:33-38`) is never populated from the task/packet. | **NONE.** `planPrompt.test.ts:36-41` tests `planPrompt` only with a hand-built `extra` object; nothing tests that `PLAN_THE_TASK.ts`'s `main()` forwards real clarify state. | Repo-agnostic. A missing function-argument wire-up — reproduces identically against any target repo. |
| 3 | Unhandled `not-resumable` exitType | **Fixed.** `writeTaskExitNotes.ts:10` `EXIT_TYPES` lists `"not-resumable"`; `IS_PREVIOUS_RUN_RESUMABLE_Q.ts:18` writes that exact value. A stale run log (`.taskTools/runs/2026-08-29T19-45-55-...-run-log.json:75`) captures the original crash (`Error: writeTaskExitNotes: unknown exit type "not-resumable"`). | **Yes.** `writeTaskExitNotes.test.ts:46-55`, `IS_PREVIOUS_RUN_RESUMABLE_Q.test.ts:50`. | Repo-agnostic. Fixed string enum, no path dependency. |
| 4 | Truncated packet JSON / agent answer not written | **Partial.** `writeAgentAnswer.ts` validates answer shape before merging but writes with a single `writeFileSync` (no write-then-rename) — a crash mid-write can still leave a truncated packet. `runStepHook.ts:273` parses packet JSON with no try/catch, so truncated JSON throws uncaught. | **NONE found.** | Repo-agnostic. The packet lives in the plugin's own run-log folder, not the target repo. |
| 5 | Worktree lease conflicts between concurrent runs | **Fixed**, two guards: `taskRunState.ts` (`adoptWorktreeLease`, `acquireAbsentWorktreeLease`) uses an intent-record-then-confirm protocol; `lockSourceRepo/LOCK_SOURCE_REPO.ts:18` `acquireSourceRepoLock(projectRoot, owner)`, staleness in `HAVE_15_MINUTES_PASSED_Q.ts`, retry loop in `WAIT_FOR_LOCK.ts`/`WAS_LOCK_ACQUIRED_Q.ts`; stale locks recover via `recoverSourceRepoLock`. | **Yes, extensive.** `sourceRepoLock.test.ts`: `test_acquireSourceRepoLock_exactlyOneWinnerAmongAcquirersReleasedFromABarrier` (270), `test_acquireSourceRepoLock_concurrentReaderNeverSeesAPartialLockDuringPublication` (297), `test_acquireSourceRepoLock_leavesNoFinalLockOrTempFileWhenTheTempWriteFails` (317); `taskRunState.test.ts` kill-mid-write tests at 521,535,547,841,856,868. Note: the `pausedRefresh` suite is known-flaky only under real concurrent load — re-run isolated before treating a failure as a regression. | None found. `LOCK_SOURCE_REPO.ts:14,18` takes `projectRoot` from the packet via `requireAbsolutePath`; the lock file lives inside the target repo, not the plugin checkout. |
| 6 | Worktrees cut from wrong base branch (should be `staging`) | **Fixed.** Commit `8376187` introduced `resolveOrCreateStagingTip`/`readStagingTip` (`scripts/prepareTasks.ts:178-186`), used at lines 439, 465, 547, 633. Commit `78b1fc1` hardened it further: missing `staging` created at HEAD, an unmerged `staging` used as-is, a merged `staging` moved to HEAD, and `staging` checked out elsewhere fails loudly (fast-forward-only). | **Yes, heavily.** `tests/prepareTasks.test.ts`: `test_createWorktreeForGroupCutsTheWorktreeFromStaging` (111), `...CreatesStagingFromHeadWhenItIsMissing` (130), `...MovesAMergedStagingToHead` (143), `...FastForwardsAMergedStagingThatIsCheckedOutElsewhere` (161), `...SelectsTheStagingBranchOverAStagingTag` (249), `test_twoPrepareProcessesWithNoStagingBranchBothStartFromTheSameStagingTip` (367). | None found. All operations use `git -C <repoRoot>` with `repoRoot`/`projectRoot` passed in as parameters, no hardcoded path to this checkout. |
| 7 | Workflow scriptPath rejection / script-path restrictions | **NOT fixed.** `scripts/tackle-tasks/shared/SkillBodyEmitter.ts:9` still hard-codes `TASK_WORKFLOW_PATH` to `../../../skills/tackle-tasks/tackle-tasks.workflow.js` (a path inside the plugin checkout), used as `scriptPath` at lines 38, 49, 88. Commit `1236a7b` only edited `.taskTools/tasks.json`, recording task 194 (still open, `tasks.json:709`) describing the intended fix (write the workflow file under `<projectRoot>/.taskTools/workflows/...` instead) — no code changed. | Test **pins the bug, not a fix**: `SkillBodyEmitter.test.ts:67` asserts `call.scriptPath` matches `/skills\/tackle-tasks\/tackle-tasks\.workflow\.js$/`, i.e. it locks in the current broken behavior. NONE for the actual fix. | **HIGH, confirmed.** Per task 194's own description, the Workflow tool rejects a `scriptPath` outside the session's working directory. Block scripts run with cwd = plugin checkout, but a target-repo session's cwd is that other repo — so this hard-coded plugin-checkout path gets rejected by the Workflow tool in any repo other than the one containing the plugin checkout. This is the one confirmed live blocker. |
| 8 | Missing staging worktree directory | **Fixed.** `scripts/tackle-tasks/shared/stagingWorktree.ts:20-28` `addOrVerifyLinkedWorktree()`: if the worktree path is missing or dead (`isLiveWorktree` check), it `rmSync`s any stale remnant then runs `git worktree add --quiet --force <path> <branch>` (line 23), which creates parent directories itself; `ensureStagingWorktree()` (line 32) calls this for the root repo and every submodule. | **Yes.** `stagingWorktree.test.ts`: `test_ensureStagingWorktree_addsALinkedWorktreeOnTheSourceBranch` (40), `test_ensureStagingWorktree_addsANestedLinkedWorktreePerSubmodule` (53) — both start from a repo with no pre-existing worktree dir. | None. Path is derived via `resolveTaskWorktreeConventionDirectory(projectRoot)` (`scripts/prepareTasks.ts:377`) from the caller-supplied `projectRoot`, not hard-coded. |
| 9 | Stop hook staging renames but not path rewrites | **Fixed, unchanged since `a696d9d`.** `commitTaskWork.ts`'s `stageOwnedChanges` uses `git status --porcelain -z --no-renames ...` and only `git add`s unstaged-change paths (the original fix for the rename-truncation and already-staged-deletion bugs). Caveat: this addresses renames within the task's owned-file fence; a path rewrite *outside* that fence is a separate, uncovered design gap (task 160 references appear to conflate the two). | **Yes** for the rename case: `commitTaskWork.test.ts:130` `test_commitTaskWork_commitsARenamedOwnedFileNextToAStagedDeletion`. NONE specifically for out-of-fence path rewrites. | None. Operates via `git -C <worktree>` on task-declared paths only — same behavior in any target repo. |
| 10 | Disk full preflight | **Not fixed — does not exist.** Searched `scripts/` and `hooks/` for `disk\|ENOSPC\|statfs\|free space\|df -`; every hit is a comment/variable name (e.g. `writeTaskBriefToDisk`). No check for available space, no `ENOSPC` handling, no `statfs`/`df` call anywhere. | **NONE.** | N/A — total absence applies identically to every repo; nothing repo-specific to break differently. |
| 11 | `.claude/settings.json` plan-mode default silently blocking `/tackle-tasks` | **Not fixed — no guard exists.** This repo's `.claude/settings.json` has no `defaultMode`/plan-mode key (its only diff vs HEAD adds an `env.CLAUDE_CODE_TASK_LIST_ID`). Searched `scripts/tackle-tasks/**` and `skills/tackle-tasks/**` for `defaultMode`/`planMode`/`plan.?mode` — zero matches; no preflight checks permission mode before launching the workflow. | **NONE.** | Same risk everywhere — since no check exists at all, any repo with a plan-mode default (in its own or the user's settings) would silently block the workflow launch identically. Not repo-specific, just universally unguarded. |
| 12 | Invalid model names in workflow/agent config | **Not fixed — no validation.** Model ids are hard-coded literals: `scripts/tackle-tasks/shared/spawnAgentCli.ts:25` (`--model claude-opus-4-8`), `:19` (`--model fable`), `CodexReviewBodyEmitter.ts:287` (`claude-opus-4-8`). Searched for `validModel`/`VALID_MODELS`/`isValidModel`/`allowedModels` across `scripts/` and `skills/` — zero matches. | **NONE.** | Not applicable / repo-agnostic — the model ids are fixed literals in shared scripts, not read from the target repo or any config the target repo controls; a bad id would fail the CLI invocation identically anywhere, uncaught by any application-level check. |
| 13 | Branch-resolution errors / checkpoint.json corruption & reversion | **Fixed for the reversion bug.** Commit `7e632ec` untracked `plans/checkpoint.json`. Confirmed in HEAD: `.gitignore:17` has `**/plans/checkpoint.json`, and `git check-ignore -v plans/checkpoint.json` returns that rule — a `git checkout`/reset no longer reverts an in-progress checkpoint in this repo. Branch-resolution ("branch did not exist") is the same failure mode as #6/#8 and is guarded by the same `resolveOrCreateStagingTip` logic. | **Indirect only.** No dedicated checkpoint-corruption test — the reversion fix is a bare `.gitignore` entry with no test of its own. Branch-resolution coverage comes from the `prepareTasks.test.ts` tests listed under #6. | **Real gap.** The ignore rule lives in *this* repo's own `.gitignore`, not shipped by the plugin. No code was found that writes a `plans/checkpoint.json` ignore entry into a *target* repo automatically — a fresh target repo could still accidentally commit its checkpoint file, or have a `git checkout` revert it, unless that repo separately ignores it. |
| 14 | `.taskTools/settings.json` diagram folder read (task 191) missing in a fresh repo | **Fixed.** `scripts/generateSteps.ts:207-217` `resolveDiagramFolderSetting(projectRoot)`: `existsSync(settingsPath) ? JSON.parse(...) : {}` (line 209), then `if (!settings.diagramFolder)` returns the built-in default (`PROJECT_ROOT/diagrams/tackle-tasks`, line 211). A missing file or missing key never throws. Introduced in commit `de7fdba` ("task 191: Read the diagram folder for tackle-tasks from .taskTools/settings.json"). | **Yes.** `tests/generateSteps.test.ts:230` `test_resolveDiagramFolderSetting_defaultsToTheRealPipelineWhenNoSettingsFile` (fresh tmp dir, no settings file); related tests at 240 (custom folder) and 251/259 (invalid-custom-folder throws). | None. `PROJECT_ROOT` (`scripts/generateSteps.ts:7`) is derived dynamically from `import.meta.url` (the plugin checkout), not hard-coded to this repo; `projectRoot` (the target repo) is passed in as a parameter, so the lookup is repo-agnostic and explicitly tested for the no-settings-file case. |
